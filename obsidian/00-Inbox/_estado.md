---
tipo: estado
fecha: 2026-08-24
estado: activo
tags: [pendiente]
---

# Estado del vault

> Claude lee este archivo al iniciar cada sesión y lo actualiza al cerrarla.

## Sesión anterior (2026-08-20)
1. Se creó `Mapa-EE-Solutions.canvas` en la raíz del vault — mapa visual de Obsidian Canvas con la matriz (`09-Organigrama`) al centro, los 14 departamentos agrupados por color en 5 clústers, el Consejo arriba, las 5 tensiones diseñadas a propósito como conexiones cruzadas, y el resto del vault conectado a su departamento dueño (`02-Clientes`/`03-Prospectos` marcados como vacíos). Ábrelo haciendo doble clic en el archivo.
2. El usuario aclaró que también quería la **vista Gráfico nativa de Obsidian** (no solo el Canvas) bien organizada. Como esa vista se arma sola a partir de `[[wikilinks]]` reales, se creó `01-Empresa/Departamentos/` con una nota por cada uno de los 14 departamentos (dueño, responde por, quién bloquea qué, coordinación dentro de su clúster, tensiones), más 3 notas índice para carpetas vacías/agrupadas (`02-Clientes/00-Indice-Clientes`, `03-Prospectos/00-Indice-Prospectos`, `06-Plantillas/00-Indice-Plantillas`). Se agregaron `[[wikilinks]]` reales en `09-Organigrama.md` (ahora es, por lejos, la nota con más enlaces — se centra sola por física de fuerzas) y en los demás documentos de `01-Empresa/`, `05-Operacion/`, `04-Marketing/`, `07-Recursos/` y `99-Archivo/` que antes vivían sueltos. Se configuró `.obsidian/graph.json` con 6 grupos de color por tag (`#empresa` gris + 5 tags de clúster `#dept-*`). Nuevos tags documentados en `CLAUDE.md` §5.
3. El usuario confirmó que el grafo ya se ve mejor y pidió (a) reasignar los pendientes sueltos a los departamentos/agentes dueños y (b) apretar más el orden visual. Se reasignaron los checkboxes de `00-Inbox/_estado.md`, `05-Operacion/Finanzas/Precios.md`, `05-Operacion/Finanzas/Cobros.md` y `05-Operacion/Reportes-Semanales/2026-08-19.md` de nombres de fundadores/"Director X" a los departamentos reales (`[[finanzas]]`, `[[legal]]`, `[[operaciones]]`, `[[growth-seo]]`, `[[contenido]]`, `[[ingenieria]]` + `[[producto-ia]]`), dejando el único ítem que sí es decisión reservada del consejo (dirección física para `LocalBusiness`, expone datos) marcado explícitamente como "Consejo (decisión reservada)". Se enlazaron las 12 plantillas desde `00-Indice-Plantillas.md` (ya no quedan notas huérfanas) y se activó `hideUnresolved` en `graph.json` para ocultar los nodos fantasma que generaban esas plantillas (`[[00-Ficha]]`, `[[01-Diagnostico]]`, etc. — no existen todavía porque no hay clientes cargados). Se subió `centerStrength` a 0.65 y se bajó `linkDistance` a 180 para compactar el layout.
4b. El usuario mostró screenshot del Canvas (`Mapa-EE-Solutions.canvas`) con cajas y grupos claramente encimados/ilegibles — confirmado: el layout manual original tenía traslapes reales (ej. `00-Contexto-Empresarial` e `_estado` invadían la caja del clúster Gobernanza). Se reconstruyó con un layout tipo "estantería" (filas que se auto-acomodan: Consejo → Matriz → núcleo de 10 docs de `01-Empresa` centrados debajo → 5 clústers de departamentos en fila → satélites propios de cada clúster anclados debajo) generado con un script Python que valida por código que ningún par de nodos se traslape antes de guardar. Mismo contenido/edges que antes, coordenadas nuevas y verificadas.
5. El usuario pidió correr una **demo end-to-end** del pipeline completo (prospecto → cliente → web entregada), y aclaró dos cosas importantes: (a) `diseno` (construye la web) y `contenido` (escribe el copy) son departamentos distintos, no un solo "creativo"; (b) el vault existe para servir clientes reales, no para auditar a EE Solutions misma (guardado en la memoria de sesiones de Claude, fuera del vault — no es una nota de Obsidian). Se creó un cliente 100% ficticio y marcado como DEMO en cada archivo: **Taller Mecánico El Tornillo** (`demo-taller-el-tornillo`) — prospecto en `03-Prospectos/`, convertido a cliente en `02-Clientes/demo-taller-el-tornillo/` con los 7 archivos completos (Ficha, Diagnóstico, Propuesta, Copy, Desarrollo, Entrega, Ficha de Pago, Bienvenida) siguiendo exactamente las plantillas reales, más `.raw/README.md` y un **`sitio-demo.html` real y funcional** (un solo archivo, paleta propia del cliente, botón de WhatsApp, schema `AutoRepair`/`LocalBusiness`) como la "web final creada". Los montos de la ficha de pago son de ejemplo (folio `DEMO-2026-001`, no un folio real `EE-####`) y **no se registraron** en `05-Operacion/Finanzas/Cobros.md` real para no contaminar el consecutivo. Se actualizaron `00-Indice-Clientes.md` y `00-Indice-Prospectos.md` para mencionar el demo sin confundirlo con clientes/prospectos reales.
6. El usuario pidió que el canvas mostrado en un segundo screenshot todavía se veía encimado/ilegible pese al fix anterior — la causa real era que las cajas de texto tenían un alto fijo (200px) que no alcanzaba para su propio contenido. Se reescribió el generador (v3) para que **cada caja calcule su alto a partir de su propio texto** (estimación de líneas envueltas + 35% de margen de seguridad) en vez de un alto fijo, y se separaron mucho más las filas. Validado sin traslapes.
7. El usuario pidió construir la web real del cliente demo usando departamentos/skills/agentes, con imágenes ficticias. Se intentó generar fotos con el MCP `higgsfield` (se llegó a enviar 2 de 4 solicitudes antes de que la cuenta se quedara sin créditos — $1.88 restantes, plan free) pero **el usuario interrumpió y pidió no usar higgsfield nunca más** (guardado en memoria de sesiones, fuera del vault). Ningún resultado de higgsfield llegó a guardarse en el vault. El usuario eligió resolver las imágenes con **ilustración propia en CSS/SVG** (sin fotos). Se reescribió `sitio-demo.html` con: íconos SVG por servicio, ilustración del hero (auto + llave), mapa ilustrado, y micro-interacciones siguiendo la skill `emil-design-eng` (easing propio, `:active` con scale, `prefers-reduced-motion`, stagger en las tarjetas de servicio). Se documentó en `04-Desarrollo.md` y `03-Copy.md` que la decisión de no usar fotos de stock viene de la regla propia de `diseno` ("nunca foto de stock"), no de una limitación técnica — las fotos reales de Rogelio siguen pendientes.
8. El usuario calificó ese primer rediseño de "genérico y feo para todas las skills y agentes que tienes" — crítica de fondo, no de detalle. Se cargó la skill `artifact-design` y se reconstruyó `sitio-demo.html` con un concepto propio del rubro: el sitio como una "orden de servicio de taller", con el tornillo hexagonal como motivo recurrente (literal a "El Tornillo"), tipografía Bebas Neue + IBM Plex Sans + IBM Plex Mono, paleta de metal/papel en vez de azul corporativo, layout de renglones de orden de servicio y placas colgadas en vez de tarjetas genéricas. Documentado en `04-Desarrollo.md` §"Concepto de diseño (v2)".
9. El usuario pidió instalar y asignar a su departamento `github.com/ConardLi/garden-skills` (5 skills). Se instaló local del vault con `npx skills@latest add ConardLi/garden-skills` (symlink en `.claude/skills/`, no global) y se asignó cada una a su departamento dueño en la tabla "Skills instaladas" de `09-Organigrama.md` y en `00-Registro-de-Agentes.md`: `web-design-engineer` → `diseno` · `gpt-image-2` → `diseno` (con nota de usar con cautela, dado el rechazo a higgsfield) · `kb-retriever` → `people` · `beautiful-article` → `contenido` · `web-video-presentation` → `marketing`. Se agregó un párrafo de uso a cada uno de esos 4 archivos de agente (`.claude/agents/diseno.md`, `marketing.md`, `contenido.md`, `people.md`), siguiendo el mismo patrón que ya usaba `diseno.md` para `emil-design-eng`.
10. El usuario pidió mejorar la web demo usando todas las herramientas, con imágenes realistas (no genéricas) y más movimiento. Se amplió `sitio-demo.html` con revelado por scroll (`IntersectionObserver`) para renglones/placas/ficha/FAQ, inclinación tipo resorte en la placa del hero siguiendo el cursor (JS con amortiguado, no CSS), una ficha de estadísticas con contador animado (9 años/6 servicios/$0 diagnóstico), y un ticker de servicios en loop — todo respeta `prefers-reduced-motion`. En el camino se corrigió un bug real (dos `transition` shorthand compitiendo por el mismo elemento). Para las fotos realistas: no hay camino disponible ahora mismo (Modo A de `gpt-image-2` necesita `OPENAI_API_KEY`, no configurada; Modo B necesitaría una herramienta nativa de imágenes y la única disponible, `higgsfield`, sigue vetada) — se escribieron 4 prompts listos para generar en `02-Clientes/demo-taller-el-tornillo/garden-gpt-image-2/prompt/taller-el-tornillo-fotos-20260820.md` (Modo C — Advisor, tal como indica la propia skill) y se le preguntó al usuario cómo quiere resolverlo. Eligió generarlas él mismo con los prompts y pasármelas. Se cableó el sitio para recibirlas sin tocar código después: `imagenes/` creada, y `sitio-demo.html` ya referencia `hero-exterior.jpg` (fondo del hero, capa detrás del degradado), `mecanico-trabajando.jpg`/`taller-interior.jpg`/`detalle-frenos.jpg` (nueva sección "Nuestro taller", 3 fotos) — con degradación elegante: si el archivo no existe, esa capa no se pinta y no se ve nada roto. Documentado en `04-Desarrollo.md` §Imágenes.
11. El usuario pidió borrar por completo el cliente demo (`demo-taller-el-tornillo` — pasos 5-10 arriba). Se eliminaron `02-Clientes/demo-taller-el-tornillo/` (los 8 archivos, `.raw/`, `garden-gpt-image-2/`) y `03-Prospectos/demo-taller-el-tornillo.md`. Se limpiaron las menciones al demo en `00-Indice-Clientes.md` y `00-Indice-Prospectos.md`. El vault queda otra vez sin clientes ni prospectos reales ni de ejemplo. Se corrió una auditoría manual de pendientes (equivalente a `/auditar`) — resultado abajo en "Pendientes de confirmar".
12. Se creó el comando `/convertir-prospecto <slug>` para que, al cerrar un prospecto, sus datos se migren a `02-Clientes/<slug>/` y el archivo de `03-Prospectos/` se borre en vez de quedar duplicado — registrado en la tabla de comandos de `09-Organigrama.md`. El usuario resolvió varios pendientes de la auditoría: precios y contrato siguen en definición; Typebot cuesta $0 (versión gratuita); WhatsApp oficial confirmado `951 212 8121` (corrige el `55 4785 0361` que tenía el vault); dirección física sí se publica en la web (decisión en `05-Operacion/Decisiones.md`); calendario de contenido se hará orgánico con `contenido`, no de golpe. Para imágenes, el usuario pidió instalar el plugin `design-assets` del repo `jezweb/claude-skills` — se instalaron sus 5 skills (`ai-image-generator`, `color-palette`, `favicon-gen`, `icon-set-generator`, `image-processing`) local del vault vía `npx skills@latest add jezweb/claude-skills -s ai-image-generator -s color-palette -s favicon-gen -s icon-set-generator -s image-processing -a claude-code --full-depth`, asignadas a [[diseno]] (documentado en `.claude/agents/diseno.md`, `00-Registro-de-Agentes.md` y la tabla de skills de `09-Organigrama.md`). `ai-image-generator` reemplaza el camino manual de imágenes (Modo C de `gpt-image-2`) usando Gemini con tier gratuito — falta configurar `GEMINI_API_KEY`.
13. El usuario pidió que `/convertir-prospecto` no solo migre datos, sino que también genere dentro de la carpeta del cliente (no en la carpeta general) tres entregables más: una infografía visual de bienvenida con logo y paleta de EE Solutions, un Agreement de una página como sustituto del contrato completo, y el desglose de la cotización. Se crearon dos plantillas nuevas en `06-Plantillas/`: `PLANTILLA-Bienvenida-Infografia.html` (HTML autocontenido, logo "EE" en SVG con el degradado de marca, usa los mismos placeholders `{{...}}` que `07-Bienvenida.md`) y `PLANTILLA-08-Agreement.md` (una página, sin las 15 cláusulas de `PLANTILLA-Contrato-OPCIONAL.md`, que sigue existiendo para el caso excepcional en que un cliente pida contrato legal completo). El desglose de cotización ya existía como `06-Ficha-de-Pago.md` — no se duplicó, solo se conectó al mismo momento de generación. Se actualizó el agente `contratacion` (ya no dice "no generas contrato" — ahora sí genera el Agreement), el comando `/contratacion` y `/convertir-prospecto` (ambos generan los 4 documentos: Ficha de Pago, Bienvenida.md, Bienvenida.html, Agreement.md — con `⟨PENDIENTE⟩` en montos/fechas si la propuesta aún no tiene precio, sin bloquear la generación), `00-Indice-Plantillas.md` y la tabla de comandos de `09-Organigrama.md`.
14. El usuario pidió correr **otro demo end-to-end**, con un negocio distinto al anterior y construyendo el sitio completo, generando las imágenes y pidiendo movimiento/animación real, no genérico. Sin `GEMINI_API_KEY`/`OPENAI_API_KEY` configuradas (se verificó el entorno, ninguna está puesta), no se pudo llamar a la API real de `ai-image-generator` — se avisó al usuario y se construyeron **ilustraciones SVG propias animadas** en su lugar (regla de `diseno`: nunca foto de stock). Se creó el cliente demo **Dental Uxmal** (`demo-dental-uxmal`, Perfil 2, Paquete Premium, García Ginerés) corriendo el pipeline completo: `03-Prospectos/demo-dental-uxmal.md` → `/convertir-prospecto` (migrado y prospecto borrado) → `01-Diagnostico.md`, `02-Propuesta.md` (montos DEMO: $24,000 producción + $1,200/mes, no registrados en `Cobros.md` real), `03-Copy.md`, `04-Desarrollo.md` (documenta el concepto de diseño y la decisión de ilustración en vez de foto) y `05-Entrega.md` (demo se detiene en Fase 4 a propósito, sin Fase 5/6 reales). Se construyó `sitio-demo.html`: identidad "Uxmal reinterpretado" con motivo de greca escalonada maya, paleta piedra/terracota/cenote/oro (deliberadamente distinta del azul clínico genérico), tipografía Fraunces + Public Sans, ilustraciones SVG animadas (arco de sonrisa con trazo animado, pirámide flotante, escena de consultorio con luz y planta animadas, 5 íconos de servicio con hover), firma de movimiento propia — reveal en cascada tipo "escalera" vía `IntersectionObserver` (distinta del tilt de resorte del demo anterior) — contadores animados, WhatsApp flotante, schema `Dentist`/`LocalBusiness`, `prefers-reduced-motion` respetado en todo. Validado: JS sin errores de sintaxis, todas las etiquetas HTML balanceadas (sin navegador disponible en esta sesión para captura visual). Se generaron también los 4 documentos de cierre con los montos demo: `06-Ficha-de-Pago.md`, `07-Bienvenida.md`, `07-Bienvenida.html` (infografía con la paleta de EE Solutions, no la del cliente) y `08-Agreement.md` — probando por primera vez la automatización creada en el paso 13. Se actualizó `00-Indice-Clientes.md` para mencionar el nuevo demo.

15. El usuario corrigió el paso 14: los tres documentos de cierre (Ficha de Pago, Bienvenida, Agreement) tienen que salir en formato **descargable de verdad**, no solo `.md`. Preguntado entre HTML con estilo de impresión (cero instalación) o una herramienta de PDF real, el usuario eligió PDF real. Se instaló **Puppeteer** (Chromium headless) fuera del vault, en `~/.claude/scripts/pdf-export/` (a propósito fuera del vault — Chromium pesa ~300MB y el vault sincroniza a iCloud). La primera descarga de Chromium quedó corrupta (postinstall bloqueado por `npm`'s allowScripts, faltaba el framework completo) — se resolvió borrando `~/.cache/puppeteer` y corriendo `npx puppeteer browsers install chrome` de nuevo, documentado como solución conocida en `00-Registro-de-Agentes.md`. Se escribió `~/.claude/scripts/pdf-export/export.js` (usa `emulateMediaType('print')`, respeta `@media print`). Se crearon dos plantillas nuevas — `PLANTILLA-Ficha-de-Pago-Documento.html` y `PLANTILLA-Agreement-Documento.html` (mismo criterio que la infografía de bienvenida: paleta y logo de EE Solutions, tablas para desglose de costos/cláusulas, `@media print`) — y se generaron los `.html` + `.pdf` faltantes del demo Dental Uxmal (`06-Ficha-de-Pago.pdf`, `07-Bienvenida.pdf`, `08-Agreement.pdf`, todos verificados como PDF válido de 2 páginas). **Se dejó la instrucción fija para el futuro:** `.claude/agents/contratacion.md`, `/contratacion` y `/convertir-prospecto` ahora exigen que los tres documentos de cierre salgan siempre en tres formatos (`.md` contenido → `.html` visual → `.pdf` descargable, este último con el comando de `export.js`), documentado también en `00-Registro-de-Agentes.md` §Herramientas locales — generación de PDF y en `00-Indice-Plantillas.md`. De paso se corrigió el WhatsApp oficial desactualizado (`55 4785 0361`) que seguía en `PLANTILLA-06-Ficha-de-Pago.md` y `PLANTILLA-07-Bienvenida.md`, cambiado a `951 212 8121`.
16. El usuario corrigió el paso 15: el `.html` de los tres documentos de cierre no debe quedarse en la carpeta del cliente, solo `.md` + `.pdf`. Se borraron `06-Ficha-de-Pago.html`, `07-Bienvenida.html` y `08-Agreement.html` del demo Dental Uxmal (los `.pdf` ya generados se conservan intactos). Se redefinió el `.html` como **paso intermedio, no como entregable**: se genera desde la plantilla, se convierte a PDF, y se borra en la misma operación (`node export.js entrada.html salida.pdf && rm entrada.html`). Actualizado en `.claude/agents/contratacion.md`, `/contratacion`, `/convertir-prospecto`, `00-Registro-de-Agentes.md` y `00-Indice-Plantillas.md` — todos ahora dicen "dos formatos finales (.md + .pdf)" en vez de "tres formatos", con el comando exacto de borrado incluido.
17. El usuario mandó una captura del logo real de EE Solutions — dos "E" itálicas, la primera sólida blanca y la segunda con el degradado de marca, sobre fondo negro. El ícono que se había usado en los 3 documentos de cierre (tres barras horizontales genéricas) no correspondía. Se construyó el SVG correcto (dos paths de "E" en bloque con `skewX(-10)` para la itálica, gradiente `eeGrad` cyan→violeta→magenta en la segunda) y se guardó como **referencia canónica** en `01-Empresa/06-Identidad-de-Marca.md` — "copiar tal cual, no reinterpretar de memoria". Se corrigió en las 3 plantillas HTML (`PLANTILLA-Bienvenida-Infografia.html`, `PLANTILLA-Ficha-de-Pago-Documento.html`, `PLANTILLA-Agreement-Documento.html`), cambiando también el fondo del badge del logo de degradado a negro sólido (así se ve en la referencia real). El usuario aprovechó para pedir otro cambio: quitar también el `.md` de los tres documentos de cierre, dejando **solo el `.pdf`** — tener `.md` + `.pdf` para lo mismo también se veía duplicado en el grafo de Obsidian. Se regeneraron los 3 PDF del demo Dental Uxmal con el logo correcto y se borraron `06-Ficha-de-Pago.md`, `07-Bienvenida.md` y `08-Agreement.md` — la carpeta del cliente quedó con exactamente `06-Ficha-de-Pago.pdf`, `07-Bienvenida.pdf`, `08-Agreement.pdf` (más las notas de trabajo Ficha/Diagnóstico/Propuesta/Copy/Desarrollo/Entrega y `sitio-demo.html`, que no forman parte de esta regla). **Instrucción actualizada para el futuro:** `.claude/agents/contratacion.md`, `/contratacion` y `/convertir-prospecto` ahora dicen que el contenido se redacta directo en el `.html` temporal (las plantillas `.md` de estos 3 documentos quedan solo como guía de estructura, no se copian al cliente), se convierte a PDF, y se borra el `.html` — un único archivo `.pdf` por documento, sin excepción. Reflejado también en `00-Registro-de-Agentes.md` §Herramientas locales y `00-Indice-Plantillas.md`.
18. El usuario reportó que el diseño se veía roto ("le quitaste formato") y que en algunos documentos no se leía el texto por los colores. Como no había forma de ver el PDF directamente, se creó `~/.claude/scripts/pdf-export/screenshot.js` (mismo Puppeteer, genera PNG con `emulateMediaType('print')` en vez de PDF) para depurar visualmente. Se encontró el bug real: `PLANTILLA-Bienvenida-Infografia.html` es de tema oscuro, pero su `@media print` volteaba el fondo general a claro sin tocar el fondo oscuro de las tarjetas (`.card`, `.contact-card`) — texto oscuro heredado sobre fondo oscuro, casi invisible. `PLANTILLA-Ficha-de-Pago-Documento.html` y `PLANTILLA-Agreement-Documento.html` se revisaron con la misma captura y **no tenían el bug** (ya se veían bien). Fix: se quitó el volteo de color de la infografía — se queda oscura también en el PDF, que es justo lo que pide el usuario ("aunque es en pdf tiene que ser una infografía la bienvenida") — y se agregó `-webkit-print-color-adjust: exact` a las 3 plantillas para que fondos/degradados impriman fieles a pantalla. Se regeneraron los 3 PDF del demo Dental Uxmal con el fix, verificados con captura antes de convertir (Bienvenida ahora totalmente legible, tema oscuro con gradiente, se ve como infografía de marca). **Instrucción para el futuro:** el paso "Verificación visual antes de dar el PDF por terminado" (screenshot → leer PNG → corregir si algo no se ve bien → recién ahí generar el PDF) quedó agregado en `.claude/agents/contratacion.md`, y el bug + la herramienta de screenshot documentados en `00-Registro-de-Agentes.md`.

19. El usuario pidió que la infografía de Bienvenida quepa en una sola hoja — con los espaciados originales (pensados para pantalla) se desbordaba a 2 páginas. Se agregó un bloque `@media print` compacto en `PLANTILLA-Bienvenida-Infografia.html` (paddings, tipografía y espaciados reducidos solo para el PDF, sin afectar cómo se vería en navegador) y se regeneró el PDF del demo Dental Uxmal — verificado con `file` (ahora dice `1 pages`) y con captura (`screenshot.js`): se sigue viendo como infografía de marca, legible, con margen de sobra al final de la hoja. **Instrucción para el futuro:** quedó documentado en `.claude/agents/contratacion.md` (punto 9 de Bienvenida + paso de verificación con `file archivo.pdf` → `1 pages`) y en `00-Registro-de-Agentes.md` — si el contenido de un cliente real es inusualmente largo, se acorta el texto antes de generar el PDF, nunca se afloja el CSS compacto.

20. El usuario, actuando como Director Financiero, cerró el modelo de precios: **2 paquetes** en vez de 3 — Esencial ($6,300 setup / $350 mes) y Completo ($8,700 setup / $550 mes) — a partir de un modelo de costos cerrado (TC $17.45, 3 clientes activos, tarifa $300/h, Typebot plan gratuito, margen 35% setup / 15% suscripción). Se creó `05-Operacion/Finanzas/01-Paquetes-de-Servicio.md` (incluye/no incluye/para quién de cada paquete) y, al pedir reconciliar todo el vault, también `05-Operacion/Finanzas/00-Precios-y-Costos.md` (registra los insumos cerrados, sin inventar el desglose de horas por paquete que no se dio — queda `⟨PENDIENTE⟩`). Se actualizaron todas las referencias al modelo viejo de 3 paquetes (Básico/Estándar/Premium): `CLAUDE.md` (frontmatter de cliente), `01-Empresa/01-Servicios-y-Paquetes.md` (tabla reescrita a 2 paquetes), `01-Empresa/03-Perfiles-de-Cliente.md` (Perfil 1 y 2 → Completo, Perfil 3 → Esencial), `05-Operacion/Finanzas/Precios.md` (precios reales cargados, desglose por componente marcado pendiente a propósito — "no inventes la proporción"), `.claude/agents/producto-ia.md` y `ventas.md`, y las 3 plantillas (`PLANTILLA-06-Ficha-de-Pago.md`, `PLANTILLA-02-Propuesta.md`, `PLANTILLA-Contrato-OPCIONAL.md`). El modelo viejo se archivó tal cual en `99-Archivo/paquetes-3-niveles-reemplazado-2026-08-21.md` (nunca tuvo precio de lista). Se registró la decisión en `05-Operacion/Decisiones.md`. El cliente demo Dental Uxmal (Perfil 2, antes "Premium") se migró a paquete **Completo** con los **precios reales** ($8,700/$550, ya no ilustrativos) en `00-Ficha.md` y `02-Propuesta.md`; se regeneraron sus 3 PDF de cierre (Ficha de Pago, Bienvenida, Agreement) reflejando el paquete y precio correctos — la Ficha de Pago ya no inventa un desglose por componente (antes repartía $24,000 en 6 líneas ficticias): ahora es un solo renglón "Paquete Completo — $8,700", consistente con la regla nueva de `Precios.md`. Los 3 PDF se verificaron con `screenshot.js` antes de convertir.

21. El usuario respondió los 6 pendientes restantes para poder contactar clientes: (1) datos bancarios — los va a ir llenando él mismo conforme mande fichas reales, así que se agregó un renglón fijo Banco/Titular/CLABE en `PLANTILLA-06-Ficha-de-Pago.md` y `PLANTILLA-Ficha-de-Pago-Documento.html`, siempre en `⟨PENDIENTE⟩`, y se instruyó al agente `contratacion` a nunca inventarlo ni copiarlo de otro lado; (2/3) el Agreement es **simbólico, no un contrato legal** — se reescribió su encabezado y su nota final en ambas plantillas y en `contratacion.md` quitando la exigencia de revisión de abogado (esa sigue aplicando solo a `PLANTILLA-Contrato-OPCIONAL.md`, la excepción); (4) el costo de dominio ya está considerado dentro del costo del paquete — resuelto en `00-Precios-y-Costos.md`; (6) se cargó la tabla de horas por fase (Esencial 12h, Completo 17h) en `00-Precios-y-Costos.md` con el cálculo mostrado (horas × $300/h vs. precio de lista → margen real ~42%/~41%, por encima del piso de 35%). Se regeneraron los PDF de Ficha de Pago y Agreement del demo con los cambios 1 y 2/3. Para el punto 5 (tutorial de `GEMINI_API_KEY`) el usuario pidió que se la instalara directamente: pegó una key con formato distinto al esperado (`AQ.Ab8...` en vez de `AIzaSy...`) — se detuvo a confirmar antes de instalarla; el usuario mandó captura de `aistudio.google.com/apikey` mostrando que el sufijo coincidía con una key real ("Gemini API Key", proyecto gratuito) — la discrepancia de formato era por desactualización del conocimiento del asistente (corte enero 2026), no un error del usuario. Se instaló en `~/.zshrc` y se probó contra la API real (`GET /v1beta/models`) confirmando respuesta válida con modelos de imagen disponibles (`gemini-3.1-flash-image`, etc.) — la sesión actual no la ve todavía, hace falta una terminal nueva.
22. El usuario pidió que la Ficha de Pago quepa en una sola hoja y que el desglose llegue hasta la mensualidad (no solo el setup). Se rediseñó `PLANTILLA-Ficha-de-Pago-Documento.html`: la tabla "Qué estás pagando" pasó de 6 filas con precio-por-componente inventado a un desglose por **característica real** del paquete (`{{filas_incluye_setup}}`, sin precio por fila, total una sola vez al final) — y se agregó el mismo criterio a "Servicio mensual" (`{{filas_incluye_mensual}}`, ej. "Cambios de contenido", "Operación del asistente"), que antes solo mostraba el precio sin desglosar. Se agregó un bloque `@media print` compacto (igual que se hizo para la Bienvenida) — se midió con Puppeteer el alto real del documento contra el alto imprimible de una hoja carta (979px) para ajustar el espaciado con precisión en vez de a ojo: primera pasada quedó en 1044px (2 páginas), se comprimió más y quedó en 849px (1 página, ~13% de margen). Se regeneró el PDF del demo Dental Uxmal con el paquete Completo desglosado en 7 características + 2 de mensualidad, verificado con `file` (`1 pages`) y captura. **Instrucción para el futuro:** quedó documentado en `.claude/agents/contratacion.md` (sección 1, Ficha de Pago) — desglosar por característica sin inventar precio por línea, desglosar también la mensualidad, y no aflojar el CSS compacto si el contenido es inusualmente largo.

23. El usuario avisó que la Ficha de Pago seguía sin mostrar que hay que pagar una suscripción mensual, y pidió que quepa en una hoja. Se encontró que "Servicio mensual" seguía etiquetado **"(opcional)"** — herencia del modelo viejo, donde el mantenimiento sí era un extra separable; en el modelo de 2 paquetes la suscripción viene junto con el setup, no es opt-in. Se corrigió en `PLANTILLA-06-Ficha-de-Pago.md`, `PLANTILLA-Ficha-de-Pago-Documento.html` (nuevo banner destacado con ambos montos justo debajo del encabezado + columna "Mensualidad" en el meta-grid), `PLANTILLA-02-Propuesta.md` y el demo — y se encontró que el **Agreement tampoco mencionaba la mensualidad en absoluto** en su cláusula de precio, también corregido en ambas plantillas. Al intentar verificar 1 página, se encontraron y corrigieron **dos bugs reales en la herramienta de verificación**: (1) `screenshot.js` medía `documentElement.scrollHeight`, que se infla hasta igualar el alto de `setViewport()` en vez de reflejar el contenido — daba falsos "se desborda"; se corrigió a medir `body.scrollHeight` y a fijar el ancho de captura en 739px (el ancho real de impresión de una hoja carta), no un ancho de ventana arbitrario. (2) La tabla de desglose se desbordaba de verdad porque la primera columna sin ancho fijo hacía salto de línea en nombres largos a 739px — se agregó `table-layout: fixed` con esa columna a 34% (`table.tabla-incluye`). **Confirmado con el PDF real (`file`, no con captura):** Ficha de Pago y Bienvenida quedaron en 1 página; el Agreement en 3 (nunca se pidió que fuera 1, no se tocó). Ambos bugs de la herramienta quedaron documentados en `00-Registro-de-Agentes.md` y `contratacion.md` (regla: la prueba definitiva de páginas es `file` sobre el PDF real, nunca la captura). PDFs del demo regenerados y verificados; `.html` intermedios borrados.

24. El usuario pidió que el Agreement también quede en una sola hoja (antes en 3 páginas, con las 10 cláusulas de contenido "de pantalla"). Se agregó el mismo tipo de bloque `@media print` compacto que ya llevaban Bienvenida y Ficha de Pago a `PLANTILLA-Agreement-Documento.html` — dos rondas de ajuste (3→2 páginas, luego 2→1) verificadas cada vez con el PDF real (`file`, no `screenshot.js`, per la lección del paso 23). Quedó en 930px de 979px disponibles, verificado también con captura para confirmar que seguía siendo legible. **Los tres documentos de cierre (Ficha de Pago, Bienvenida, Agreement) ya caben en 1 página cada uno.** Instrucción agregada en `.claude/agents/contratacion.md` (punto de Agreement).

25. El usuario pidió borrar el cliente demo (`demo-dental-uxmal` — pasos 14-24 arriba) y todos sus registros. Se eliminó `02-Clientes/demo-dental-uxmal/` completa (las 6 notas de trabajo, `.raw/`, `sitio-demo.html`, y los 3 PDF de cierre ya en 1 página cada uno). Se limpió la mención al demo en `00-Indice-Clientes.md`. El vault queda otra vez sin clientes ni prospectos, reales ni de ejemplo. Las plantillas, comandos, agentes y el modelo de 2 paquetes que se construyeron/ajustaron probando con este demo (§13-24) **no se tocaron** — son infraestructura del vault, no parte del cliente ficticio, y siguen listas para el primer cliente real.

26. El usuario confirmó que **no existe desglose por componente** — solo hay precio por paquete completo (Esencial/Completo), y eso es definitivo, no un dato por cargar después. Se cerró en `Precios.md` (§Desglose por componente reescrito como "decidido: no existe", los 3 checkboxes de Pendientes marcados `[x]`, `estado: parcial → vigente`, tag `pendiente` quitado) y en `00-Precios-y-Costos.md` (movido de "Qué sigue sin cerrar" a una línea "Decidido, no pendiente"). También se corrigió de paso el costo de dominio en `Precios.md`, que seguía marcado `⟨PENDIENTE⟩` en dos lugares pese a haberse cerrado antes (ya incluido en el costo del paquete). Con esto, el único pendiente real que queda en todo el vault es cargar clientes/prospectos reales — que es la acción de prospectar en sí, no un dato faltante.

27. El usuario pidió juntar todos los pendientes en un solo apartado — venían repetidos y desactualizados entre `_estado.md`, `Precios.md` y `Cobros.md` (ej. el costo de dominio seguía `⟨PENDIENTE⟩` en un lado aunque ya se había cerrado en otro, paso 26). Se creó `05-Operacion/00-Pendientes-Generales.md` como fuente única: sección "Generales — empresa" (los 2 pendientes vivos: datos bancarios y cargar clientes/prospectos reales) + sección "Por fase de cliente" con un encabezado por cada una de las 6 fases, vacía por ahora, para cuando haya clientes reales + sección "Resueltos recientes" para no perder el historial de qué se cerró y cuándo (mismo criterio que `Decisiones.md`: no se borra, se tacha). Se quitaron las secciones `## Pendientes` de `Precios.md`, `Cobros.md` y `_estado.md` — cada una ahora enlaza a la nota consolidada en vez de mantener su propia lista.

28. El usuario preguntó por un nodo "Untitled" flotando en el grafo y por qué aparecían cosas sin conectar. Se corrió un análisis completo de todo lo que Obsidian realmente indexa (57 notas/canvas/base, excluyendo `.claude`/`.agents` que ya están ocultos por ser dotfolders) resolviendo wikilinks por "shortest path" igual que hace Obsidian, y se encontraron 5 huérfanas exactas: `00-Inbox/Untitled.base` (stub vacío sin configurar), `Untitled.canvas` y `Untitled 1.canvas` (ambos `{}`, creados con un minuto de diferencia — clic accidental), `Welcome.md` (nota default de Obsidian, literalmente dice "delete this note") y `CLAUDE.md` (el contrato operativo completo del vault — sí sirve, solo que nada le hace wikilink porque se referencia con rutas en backticks). Las primeras 4 se borraron. `CLAUDE.md` se dejó intacto y se ocultó **solo visualmente** del grafo nativo, agregando `-path:"CLAUDE.md"` al filtro de búsqueda en `.obsidian/graph.json` — no se le agregaron wikilinks ni se tocó su contenido, tal como pidió el usuario ("los que sí sirvan, quítalos visualmente solamente").

29. El usuario pidió que esto sea una regla permanente, no una limpieza única: de ahora en adelante, cualquier nota nueva que aparezca huérfana en el grafo se resuelve igual que en el paso 28 — conectarla con wikilink si tiene sentido, ocultarla solo del grafo si es infraestructura legítima que no necesita aparecer (como `CLAUDE.md`), o borrarla si no sirve para nada. Se agregó como regla fija en `CLAUDE.md` §5 "Convenciones de escritura en el vault", con el precedente del paso 28 citado como referencia.

30. El usuario pidió diseño para el grafo: los 14 sub-departamentos deben verse del mismo color que su clúster (departamento) pero en un tono más claro, para distinguirse entre sí sin perder la familia de color. Antes, `graph.json` coloreaba por tag de clúster (`#dept-comercial`, etc.) — los 3 departamentos de un mismo clúster (ej. ventas/marketing/growth-seo) salían exactamente del mismo color, indistinguibles entre sí. Se generó una paleta de 14 tintes (uno por sub-departamento) manteniendo el matiz (hue) y la saturación del color base de su clúster, subiendo la luminosidad progresivamente por miembro (+9% cada uno, tope 83%) — calculado con HLS, no a ojo. Se agregaron 14 `colorGroups` nuevos en `.obsidian/graph.json`, con query `path:"Departamentos/<slug>"` (más específico que el tag de clúster, así que gana para esos 14 nodos exactos) después de los 5 grupos de clúster existentes, que se dejaron intactos como respaldo por si algún día otro archivo no-departamento usa esos tags. Paleta resultante: Comercial (magenta) ventas `#FF5CA0` → marketing `#FF8ABB` → growth-seo `#FFA8CC` · Creativo (naranja) contenido `#FFB247` → diseño `#FFC675` · Producto (cyan) ingeniería `#49D3EE` → producto-ia `#73DDF2` · Gobernanza (violeta) finanzas `#A47ADC` → legal `#BD9EE5` → datos `#D0BBED` · Operación (verde) operaciones `#6ABE6D` → contratación `#8ACC8C` → soporte `#AADAAC` → personas `#C3E5C4`.

31. El usuario reportó que los sub-departamentos se seguían viendo iguales en el grafo pese al paso 30. Causa probable: los 5 grupos por clúster (`tag:#dept-comercial`, etc.) seguían en `graph.json` compitiendo con los 14 nuevos por sub-departamento (`path:"Departamentos/<slug>"`) — como todo departamento tiene ambos (el tag de su clúster y su path específico), y el clúster listaba primero en el arreglo, probablemente ganaba esa precedencia y el tinte más específico nunca se aplicaba. **Fix:** se quitaron los 5 grupos de clúster por completo — no se usan en ningún otro archivo del vault (confirmado por grep), así que no hacía falta mantenerlos como respaldo. Ahora cada departamento matchea un solo grupo, sin ambigüedad posible. De paso se hicieron los tonos más claros en general (luminosidad base subida) y con más separación entre sí (paso de 9% a 14% de luminosidad por integrante del clúster) para que la diferencia se note de verdad en nodos pequeños, no solo en el cálculo.

32. **El usuario dio de alta al primer cliente real de EE Solutions: AME Events**, recinto para eventos en Montes de Amé, Mérida (4.8★, 38 reseñas en Google), pasando el texto completo de su ficha de Google Maps. Se guardó tal cual en `.raw/google-maps-listing-20260823.md` y se construyó `02-Clientes/me-events/` completo hasta Fase 4: `00-Ficha.md` (Perfil 1, paquete Completo, todo lo no confirmado marcado `⟨PENDIENTE⟩` — sin nombre de dueña, sin horario, sin tipos de evento/capacidad), `01-Diagnostico.md`, `02-Propuesta.md` (precio real de lista: $8,700 + $550/mes), `03-Copy.md` (usa las 4 reseñas reales citadas con autor — nunca inventadas — y **omite a propósito una sección de "Servicios" con tipos de evento/capacidad porque ese dato no está confirmado**, en vez de rellenarla con contenido genérico). Se construyó `sitio-demo.html`: concepto "golden hour celebration" (luces cálidas tipo string-lights, paleta noche/oro/blush, tipografía Fraunces + Public Sans), reseñas reales citadas con autor, schema `EventVenue` con `aggregateRating` real, firma de movimiento propia (fade-in tipo "vela encendiéndose", distinta a los dos clientes demo anteriores), JS y HTML validados (balance de etiquetas, sintaxis). **Se intentó por primera vez generar imágenes reales con Gemini** (`gemini-3.1-flash-image` y `gemini-2.5-flash-image`) y ambos fallaron con `429` y cuota gratuita **0** — no es un límite agotado, es que el tier gratuito no cubre generación de imágenes para esta cuenta, contradiciendo la documentación previa de la skill. Se documentó como bug conocido en `00-Registro-de-Agentes.md` y se actualizó `.claude/agents/diseno.md` para que no se vuelva a intentar sin antes confirmar que se habilitó facturación. Se construyó ilustración SVG propia en su lugar (guirnalda de luces animada, lámpara flotante, campo de bokeh) — además de ser el único camino que funciona hoy, es la opción más honesta: no hay fotos reales del recinto todavía, y una "foto" de IA que pareciera documentar el salón exacto habría sido engañosa para un cliente real. Se actualizó `00-Indice-Clientes.md` con la tabla de clientes activos (ya no dice "sin clientes").

33. El usuario notó que faltaban los documentos de cierre de AME Events (Ficha de Pago, Bienvenida, Agreement) — se habían omitido por esperar a que el trato estuviera formalmente cerrado, cuando la regla real del vault (`contratacion.md`) dice que se generan igual con el precio de lista, marcando `⟨PENDIENTE⟩` solo lo que de verdad falta. Se generaron los 3 con el **folio real EE-2026-001** (primer folio real del vault, registrado en `05-Operacion/Finanzas/Cobros.md` como emitido pero no pagado — el anticipo no está confirmado) y datos reales de AME Events: paquete Completo, $8,700 + $550/mes, desglose por característica (no por precio inventado), banco/titular/CLABE y nombre de la dueña en `⟨PENDIENTE⟩` porque no se conocen. Los 3 quedaron en 1 página cada uno (902-925px de 979px disponibles), verificados con `file` y captura antes de borrar los `.html`. Se agregó una sección "Documentos de cierre" en `00-Ficha.md` de AME Events con el folio y el recordatorio de actualizar `fase` cuando se confirme el anticipo.

34. El usuario reportó que el sitio de AME Events "se veía muy genérica" y pidió investigar sitios reales del mismo rubro usando las skills disponibles. Se leyó `SKILL.md` de `web-design-engineer` y se diagnosticó con `references/failure-patterns.md` el problema real: cardification (cada dato en tarjeta redondeada), fórmula de "eyebrow" repetida antes de cada sección, e ilustración SVG de escena (guirnalda/lámpara/mesas) sustituyendo una foto real — justo el patrón que la skill marca "casi nunca". Se investigaron sitios reales de venues (WebSearch) y se rediseñó `sitio-demo.html`: hero asimétrico con placeholder de foto honesto y rotulado (no escena fingida), "ficha del recinto" como dossier numerado sin cards, reseñas como pull-quotes editoriales apiladas, tipografía cambiada de Fraunces (en la lista de fuentes sobreusadas por IA) a Cormorant Garamond, botones sin pill/glow. Documentado con tabla Antes/Después/Por qué en `04-Desarrollo.md`.
    - El usuario pidió además cambiar la paleta y anexar un video real (link corto de Google Maps). Al resolver el link se encontró que la ficha de Maps dice **"AME Events"**, no "ME Events" como estaba en todo el vault — se preguntó y el usuario confirmó "AME Events". Se corrigió en cascada: carpeta renombrada `me-events/` → `ame-events/`, ficha/propuesta/copy/desarrollo/índice/Cobros/folio EE-2026-001/sitio, y se regeneraron los 3 PDF de cierre con el nombre correcto (`.raw/` original no se tocó, conserva "ME Events" tal como se transcribió). Paleta cambiada a añil/terracota (grafito-índigo + naranja óxido, referencia real de material yucateco) y se agregó sección de video con link directo a Maps (no hay forma pública de embeber el archivo).
    - El usuario mandó una referencia real (`haciendateya.com`) y una carpeta con 5 fotos reales del recinto, pidiendo basarse en esa web sin copiarla. Se tomó el principio estructural (foto real a sangre en el hero + galería propia) sin copiar colores/tipografía/copy. Las fotos se guardaron intactas en `.raw/fotos-videos-20260824/` y se usaron versiones optimizadas en `assets/`. Una de las 5 fotos era el logo de "La 34 · M. de A. · Events & more" — un tercer nombre posible; se preguntó de nuevo y el usuario confirmó que es otro espacio del mismo predio, AME Events no cambia. Bug real encontrado en QA visual (no solo revisión de código, se tomaron capturas con Puppeteer scrolleando la página real): la galería usaba `grid-auto-rows: 1fr` que colapsaba a alto 0 en un grid sin alto definido — corregido a `grid-template-rows: auto auto`.
    - El usuario reportó que la paleta añil/terracota "está fea" y pidió colores más neutros — se cambió a un sistema grafito/bronce (`--noche #211E1B`, `--acento #7C6448`, `--acento-alta #C9B896`), contraste verificado a mano (todas las combinaciones ≥4.5:1), verificado visualmente. Historial completo de las 4 vueltas de paleta documentado en `04-Desarrollo.md`.

35. El usuario pidió duplicar los pendientes por fase de cliente dentro de la carpeta de cada cliente (para verlos sin salir de esa carpeta), además de en la nota general. Primero se preguntó por una parte confusa del pedido ("RRHH") y el usuario dijo que la olvidara, aclarando que solo quería el espejo por cliente. Se agregó una sección "Pendientes de este cliente" en `00-Ficha.md` de AME Events, espejo exacto de sus filas en `00-Pendientes-Generales.md`, con la regla de sincronía explícita (se edita en los dos a la vez; al resolver, se tacha en ambos y se borra del espejo, el historial vive solo en la nota general). Se actualizó `CLAUDE.md` para agregar esta única excepción a la regla de "nunca crees `## Pendientes` en otro archivo". Confirmado que ambas listas coinciden.

36. El usuario preguntó por `99-Archivo` (¿seguía en 3 paquetes?) y por `PLANTILLA-Contrato-OPCIONAL.md` (¿por qué sigue si solo se usa Agreement?). Se investigó ambas cosas antes de tocar nada: `99-Archivo/paquetes-3-niveles-reemplazado-2026-08-21.md` ya estaba correctamente archivado con banner y fecha — no hacía falta ningún cambio, se le explicó al usuario con evidencia. El Contrato-OPCIONAL sí resultó ser una ruta de escalamiento deliberada, documentada en `contratacion.md` y en el Agreement mismo — se preguntó al usuario si quería archivarla o dejarla como excepción; el usuario interrumpió la pregunta para pedir otra cosa (cambio de paleta), así que **quedó sin resolver** — no se tocó ese archivo.

37. **Segundo cliente real: Romelia Bakery**, restaurante de brunch en Colonia Altabrisa, Mérida (4.8★, 288 reseñas, $300-400/persona). Se guardó la ficha de Maps tal cual en `.raw/google-maps-listing-20260824.md` y se construyó `02-Clientes/romelia-bakery/` con Ficha, Diagnóstico, Propuesta y Copy (Perfil 1, paquete Completo, mismo precio de lista que AME) usando solo datos reales — 3 reseñas citadas con autor, plato insignia (chilaquiles) identificado por el propio etiquetado de Google, sección de Menú explícitamente `⟨PENDIENTE⟩`. El usuario avisó que mandará una carpeta con menú y fotos reales **antes** de construir el sitio — `04-Desarrollo.md` se dejó como nota de espera (`estado: en-espera`), sin `sitio-demo.html`, para no inventar carta ni usar imágenes de relleno cuando el material real ya viene en camino. Registrado en `00-Indice-Clientes.md` y en `00-Pendientes-Generales.md` (con su propio espejo en `00-Ficha.md`, siguiendo la regla del paso 35).

38. El usuario mandó la carpeta prometida de menú y fotos reales de Romelia Bakery (9 capturas: 4 de la carta física con ~25 platillos y precio real, 5 del lugar/comida) y confirmó que el cliente eligió el **paquete Completo**. Copiadas sin editar a `.raw/menu-fotos-20260824/`, optimizadas en `assets/`. Se completó `03-Copy.md` con el menú real (categorías Desayunos/Chilaquiles/Bagels/Bebidas, sin inventar ningún precio) y se reescribió `04-Desarrollo.md` (antes stub "en-espera") con el build real: concepto "editorial de panadería en modo claro" — deliberadamente distinto a AME Events (fondo claro vs oscuro, justificado por la fachada real fotografiada), wordmark en fuente script (Alex Brush) evocando el letrero real sin inventar un logo vectorial que no existe (Placeholder Philosophy), paleta piedra/caramelo con un solo acento, hero con foto real enmarcada (no a sangre, para no tapar el letrero real con texto encimado), menú como sección propia sin cards. Se verificó visualmente con Puppeteer (scroll completo) antes de dar el sitio por terminado — sin repetir el bug de grid de AME Events. Se generaron los 3 documentos de cierre con folio real **EE-2026-002** ($8,700 + $550/mes, mismo paquete que AME), verificados en 1 página cada uno. Actualizados `00-Ficha.md` (fase 1→4, pendientes resueltos tachados en espejo y en la nota general), `00-Indice-Clientes.md` y `Cobros.md`.

39. El usuario pidió el menú **completo**, no la selección curada del paso 38. Se releyeron las 4 fotos de la carta (verificación por segunda vez, no solo memoria) y se transcribió todo: Desayunos (9), Chilaquiles (3), Burritos (3, categoría nueva que no estaba en la versión curada), Sandwiches y bagels (4), Tradicionales (3), Extras (13, en lista compacta 2 columnas) y Bebidas con sus 4 subcategorías (Naturales/Calientes/Frías/Refrescos, 30 renglones) — ~65 líneas en total, todas verificadas contra la foto. Se documentó honestamente una inconsistencia real de la carta física: "Encremadas Xcatic" aparece a $235 en Desayunos y a $250 en Tradicionales — no se corrigió ni se adivinó cuál es la correcta, se transcribió tal cual con nota aclaratoria en `03-Copy.md`. Se agregó un nav de anclas al inicio de la sección Menú del sitio para navegar la carta ampliada. Verificado con Puppeteer (render completo) que todo el contenido nuevo se ve correctamente.

40. El usuario pidió ordenar el vault revisándolo por partes, empezando por `00-Inbox/`. En el camino se detectó que `05-Operacion/Finanzas/00-Precios-y-Costos.md` listaba "Clientes activos considerados: 3" — se investigó y se confirmó que es un insumo histórico del modelo de costos (cerrado 2026-08-21), no un contador en vivo. El usuario pidió borrar por completo los clientes activos (AME Events y Romelia Bakery) y los prospectos porque "actualmente no tenemos ninguno". Antes de borrar se preguntó por alcance/respaldo (el vault no tiene git) y se hizo explícito que la convención propia del vault (`CLAUDE.md` §3, mapa de carpetas) dice que los proyectos cerrados se mueven a `99-Archivo/` y nunca se borran — el usuario, informado de esto, insistió en borrado permanente sin respaldo. Se eliminaron `02-Clientes/ame-events/` y `02-Clientes/romelia-bakery/` completos (fichas, diagnóstico, propuesta, copy, desarrollo, 3 PDF de cierre cada uno, fotos, `sitio-demo.html`). Se actualizaron en cascada: `00-Indice-Clientes.md` (sin clientes activos), `05-Operacion/Finanzas/Cobros.md` (se quitaron los folios EE-2026-001 y EE-2026-002), `05-Operacion/00-Pendientes-Generales.md` (se quitaron los 13 pendientes abiertos de ambos clientes) y `00-Precios-y-Costos.md` (Clientes activos considerados: 3 → 0, en dos pasadas porque la primera corrección a "2" quedó obsoleta en cuanto se confirmó el borrado total). De paso se borraron 9 archivos `.DS_Store` sueltos en todo el vault (metadata de Finder sin contenido real). **Queda sin resolver:** `Mapa-EE-Solutions.canvas` se modificó el 2026-08-27 sin quedar documentado en ningún lado — falta confirmar con el usuario qué cambió ahí. **Regla nueva confirmada por el usuario:** de ahora en adelante cada sesión de trabajo sobre este vault se registra aquí (esto ya era la regla en `CLAUDE.md` §0, pero no se estaba cumpliendo en tiempo real durante la sesión — el usuario pidió reforzarlo).

41. El usuario pidió revisar `Mapa-EE-Solutions.canvas` (modificado 2026-08-27 sin documentar) — se encontraron dos bloques de texto desactualizados ("📂 03-Prospectos/" y "📂 02-Clientes/") que seguían mencionando "1 demo (ficticio)" cuando esos demos ya se habían borrado antes de esa fecha, y sin mencionar que en ese momento sí había 2 clientes reales cargados. Al revisar esto, **el usuario corrigió algo más de fondo: AME Events y Romelia Bakery tampoco eran clientes reales — eran demos, igual que `demo-taller-el-tornillo` y `demo-dental-uxmal`. Nunca ha habido un cliente real en EE Solutions.** Esto contradice cómo se documentaron en su momento (pasos 32-39 arriba los llaman "primer/segundo cliente real de EE Solutions"). Siguiendo la regla del vault de no borrar historial sino anotar la corrección: se dejaron los pasos 32-39 intactos (documentan fielmente lo que se hizo y se creyó en su momento) y se agregaron notas de corrección fechadas 2026-08-29 en los dos renglones de `05-Operacion/00-Pendientes-Generales.md` que los llamaban "primer/segundo cliente real", en `07-Recursos/Agentes-y-Herramientas/00-Registro-de-Agentes.md` (que decía "el primer cliente" al documentar el bug de cuota de Gemini, y tenía además una ruta rota `02-Clientes/me-events/...` de antes del rename), y se corrigieron los dos textos del canvas para que digan "sin clientes/prospectos reales todavía" sin mencionar demos específicos como si quedara uno vivo. **Estado real y definitivo: EE Solutions no ha tenido ningún cliente real, solo demos de práctica, todos borrados.**

42. Al pasar a revisar `01-Empresa/` se detectó que la carpeta se había renombrado a `Empresa/` (sin el prefijo numérico) — Obsidian actualizó solo el canvas (`Mapa-EE-Solutions.canvas`, wikilinks/paths internos), pero quedaron 18 archivos con la ruta vieja en texto plano (`CLAUDE.md`, 8 agentes/comandos de `.claude/`, `_estado.md`, `09-Organigrama.md`, `00-Registro-de-Agentes.md`, `Precios.md`, `00-Indice-Plantillas.md`) que no se habrían actualizado solos. El usuario confirmó que el rename fue accidental y pidió revertirlo. Se regresó la carpeta a `01-Empresa/` y se corrigieron las 9 referencias del canvas que Obsidian había cambiado a `Empresa/...` de vuelta a `01-Empresa/...`. Los 18 archivos en texto plano nunca se rompieron (seguían diciendo `01-Empresa` todo el tiempo), así que no necesitaron tocarse.

42. **Reestructuración completa del organigrama, departamento por departamento**, pedida por el usuario tras revisar cada uno de los 14. Cambios:
- **`ventas`, `finanzas`, `legal`:** confirmados "perfecto" — sin cambios.
- **`marketing` absorbe `contenido`** (eran, en la práctica, la misma función): nuevo mandato incluye copy, calendario editorial y voz de marca; se agrega el **programa de contenido semanal**, anexado al reporte de los domingos; se reasigna la skill `beautiful-article` de `contenido` a `marketing`. Se borraron `.claude/agents/contenido.md` y `01-Empresa/Departamentos/contenido.md`.
- **`ingenieria` absorbe `growth-seo`** (palabras clave, NAP, Google Business, reseñas, Search Console): el SEO técnico y de negocio se ejecuta junto con el código. Se agregó la generación de `sitemap.xml` + su vínculo a Search Console como paso obligatorio del checklist de deploy (documentado como automatizable en la generación, manual en la vinculación a la cuenta del cliente). Se borraron `.claude/agents/growth-seo.md` y `01-Empresa/Departamentos/growth-seo.md`.
- **`diseno`:** se formalizó un checklist obligatorio (no a discreción) de skills en orden — `web-design-engineer` antes de CSS, generación de imágenes/ilustración animada obligatoria en cada sitio (nunca estático), `emil-design-eng`, `color-palette`, `favicon-gen`, `icon-set-generator`, `image-processing`.
- **`producto-ia`:** debe leer `00-Ficha.md`/`01-Diagnostico.md`/`03-Copy.md` del cliente activo antes de diseñar el flujo de Typebot, para que el diagrama salga del negocio real, no de una plantilla genérica.
- **`datos`:** extiende su mandato a recopilar y consolidar el reporte semanal de los domingos (además de su reporte mensual de KPIs).
- **Se crea el departamento `gerencia`** (nuevo, `.claude/agents/gerencia.md` + `01-Empresa/Departamentos/gerencia.md`): capa entre los 12 departamentos operativos y el consejo. Revisa el trabajo agregado (empezando por el reporte semanal) con criterio de administración de empresa y lo autoriza — no repite las decisiones de cada departamento. El consejo (Emilio y Eduardo, fundadores y directores) queda arriba de todo, pero solo para las cinco decisiones reservadas.
- **`.claude/agents/_ESTATUTO.md`** (el archivo real que heredan los 14 agentes) se actualizó con los "tres niveles": departamentos → gerencia → consejo.
- **`CLAUDE.md` se sube a v3:** se descubrió que §1 (8 funciones fijas 4/4 por fundador) llevaba desincronizado de la realidad desde que la empresa creció a 14 departamentos autónomos vía `_ESTATUTO.md` — nunca se había actualizado. Se reescribió §1 para remitir a `01-Empresa/09-Organigrama.md`/`_ESTATUTO.md` en vez de duplicar la tabla, se reemplazó el modelo "todo es borrador hasta validación de un fundador" por los tres niveles, y se agregó el Anexo de decisiones v2→v3. Se corrigieron además la tabla de handoff (§12, Contenido→Marketing) y el conteo de agentes en subagentes (§13).
- **`01-Empresa/09-Organigrama.md`** reescrito completo: tabla de 12 departamentos + Gerencia, cobertura de servicios, tensiones y "quién puede detener qué" actualizadas a los departamentos fusionados.
- **Referencias cruzadas corregidas** en `01-Servicios-y-Paquetes.md`, `05-Tono-de-Voz.md`, `reel-3-senales-perdiendo-clientes.md`, `00-Registro-de-Agentes.md` (dueño de `beautiful-article`, y la descripción del job `reporte-semanal` para incluir a `datos`, `marketing` y `gerencia`), y los agentes `ventas`, `soporte`, `operaciones`, `datos`, y los comandos `/vigilancia` y `/junta`.
- **`Mapa-EE-Solutions.canvas`:** se quitaron los nodos de "Contenido y Marca" y "Growth y SEO", se actualizó el texto de los nodos de Marketing e Ingeniería para reflejar las fusiones, se agregó el nodo de Gerencia entre el Organigrama y el Consejo, y se re-cablearon las aristas correspondientes (validado sin aristas rotas).
- **`.obsidian/graph.json`:** se quitaron los `colorGroups` de `growth-seo` y `contenido`, se agregó uno para `gerencia` (dorado, para distinguirlo de los 5 clústers operativos).

**Nota:** en el camino se cometió y se corrigió un error de sintaxis JSON propio en `graph.json` (comilla faltante al editar, corregido de inmediato y validado con `python3 -c "json.load(...)"`).

**Corrección en la misma sesión:** el usuario reportó que el grafo real de Obsidian seguía mostrando todo colgado de `09-Organigrama` y que `gerencia` casi no aparecía. Causa: solo se había actualizado la frase de enlace a `[[gerencia]]` en `marketing.md` e `ingenieria.md` — los otros 10 departamentos se quedaron con la frase vieja que solo enlazaba a `[[09-Organigrama]]`, sin mencionar a Gerencia. Se corrigieron los 10 restantes (`ventas`, `diseno`, `finanzas`, `legal`, `datos`, `operaciones`, `contratacion`, `soporte`, `people`, `producto-ia`) para que todos enlacen también a `[[gerencia]]` — verificado con grep que los 12 departamentos operativos ahora enlazan a `gerencia` y ninguno conserva la frase vieja.

43. Continuó la revisión sección por sección del vault (pedida por el usuario). Cambios menores encontrados y corregidos:
- `01-Empresa/04-Proceso-6-Fases.md`: la tabla de fases usaba títulos viejos de v2 ("Dir. Contenido", "Dir. Desarrollo", etc.) — corregida a los departamentos y fundadores reales.
- `04-Marketing/Posts/reel-3-senales-perdiendo-clientes.md`: sugería generar la versión en imagen con `higgsfield` (vetado) — corregido a ilustración SVG/CSS de `diseno`. Aparte, se coló un "si" suelto en el archivo (tecleo accidental, probablemente el chat escribiendo sobre el archivo abierto en Obsidian) — quitado a petición del usuario.

51. El usuario pidió borrar el cliente demo (`demo-veterinaria-cocom` — pasos 47-50 arriba) y todos sus registros, mismo criterio que con `demo-taller-el-tornillo` y `demo-dental-uxmal` (pasos 11 y 25). Se eliminó `02-Clientes/demo-veterinaria-cocom/` completa (Ficha, Diagnóstico, Propuesta, Copy, Desarrollo, `.raw/`, `assets/`, `sitio-demo.html`, `sitemap.xml`, `robots.txt`, y los 3 PDF de cierre folio DEMO-2026-003). Se corrió una búsqueda de "cocom"/"demo-veterinaria"/"DEMO-2026-003" en todo el vault (`.md`, `.canvas`, `.json`, `.html`) para confirmar que no quedaran rastros activos: `00-Indice-Clientes.md`, `05-Operacion/00-Pendientes-Generales.md`, `05-Operacion/Finanzas/Cobros.md`, `Mapa-EE-Solutions.canvas` y `.obsidian/graph.json` ya estaban limpios (nunca llegaron a mencionar este demo). Se limpiaron las 14 entradas de `.obsidian/workspace.json` §`lastOpenFiles` que apuntaban a archivos ya borrados de este demo (más una entrada suelta `demo-veterinaria-peninsular`, cruft sin archivo real detrás) — validado que el JSON sigue siendo válido. **No se editó** `05-Operacion/Reportes-Semanales/2026-08-30.md` pese a mencionar el demo como existente: es una nota de snapshot histórico (`estado: vigente` pero fechada, igual que `2026-08-19.md`), describía la realidad correcta del día en que se escribió — mismo criterio que ya se aplicó a reportes previos, se corrigen con nota fechada solo cuando el contenido original era un error, no cuando el mundo cambió después. Este único rastro queda documentado aquí a propósito para quien lea ese reporte más adelante. El único rastro real que sí queda, y debe quedar, es este mismo párrafo (bitácora de sesión — nunca se borra, se documenta).
- `02-Clientes/.DS_Store`: Finder lo regeneró tras el borrado de la sesión anterior — borrado de nuevo.
- `05-Operacion/Finanzas/01-Paquetes-de-Servicio.md`: el usuario confirmó que el alcance de los 2 paquetes ya está validado — se quitó `estado: borrador`/tag `validar` (ahora `vigente`) y el banner `⚠️ Pendiente de validación humana`.
- `05-Operacion/Reportes-Semanales/2026-08-19.md`: marcado `estado: archivado` con nota de que sus pendientes ya se migraron a `00-Pendientes-Generales.md` — se deja el contenido histórico intacto (incluye menciones a `contenido`/`growth-seo`, ya fusionados).

`01-Empresa/` y `05-Operacion/` quedan revisados por completo. Sigue pendiente: `06-Plantillas/`, `07-Recursos/`, `99-Archivo/`.

44. Revisión de `06-Plantillas/` (16 plantillas + índice). Encontrado y corregido:
- Canvas: nodo de `06-Plantillas/` decía "12 plantillas base" — corregido a 16, coincidiendo con `00-Indice-Plantillas.md` (que ya estaba bien).
- `PLANTILLA-Contrato-OPCIONAL.md` cláusula DÉCIMA: seguía diciendo que el mantenimiento es "opcional y se contrata por separado" — la misma herencia del modelo de 3 paquetes que ya se había corregido en Ficha de Pago y Propuesta (paso 23) pero no aquí, porque este archivo casi no se usa (es la excepción para contrato legal completo). Corregido: el mantenimiento va incluido en el paquete.
- `PLANTILLA-00-Ficha.md`: le faltaba la sección `## Pendientes de este cliente` que `CLAUDE.md §0` establece como estándar para todo cliente activo desde el 2026-08-24 — se agregó a la plantilla base para que no dependa de que alguien se acuerde de crearla a mano.
Se verificó que las demás plantillas (Propuesta, Ficha de Pago, Agreement y sus 3 versiones HTML) no tuvieran el mismo problema de "opcional" — limpias.

45. Revisión de `07-Recursos/`. Encontrado y corregido:
- `00-Registro-de-Agentes.md` decía "14 departamentos" — corregido a 13 (conteo real verificado con `ls .claude/agents/*.md`, sin contar `_ESTATUTO.md`).
- Se creó `05-Operacion/SEO/` (vacía, con `.gitkeep`) — el registro la menciona como destino de reportes de auditoría SEO pero nunca se había creado.
- **Se encontró una decisión real sin registrar:** `00-Registro-de-Agentes.md` cita "la nota de decisión del 2026-08-19 sobre automatización local vs. cloud", pero esa decisión nunca se escribió en `05-Operacion/Decisiones.md` — solo existía como memoria de sesión de Claude, fuera del vault. Se agregó la fila faltante a `Decisiones.md` (automatización recurrente corre 100% local vía `launchd`, nunca en la nube, por privacidad de datos de clientes/operación).

46. Revisión de `99-Archivo/` — última carpeta pendiente. Ambos documentos archivados están correctos (banner de "archivado" apuntando a la fuente vigente, contenido histórico intacto sin editar, tal como manda la regla del vault). No requirió cambios. **Con esto se cierra la revisión completa del vault, sección por sección, pedida por el usuario** — cubrió `00-Inbox`, `01-Empresa` (incluida la reestructuración completa de departamentos + creación de `gerencia` + `CLAUDE.md` v3), `02-Clientes`, `03-Prospectos`, `04-Marketing`, `05-Operacion`, `06-Plantillas`, `07-Recursos`, `99-Archivo`, y el canvas/grafo.

47. El usuario pidió "explorar" usando todas las skills de EE Solutions, para probar en la práctica el checklist que se formalizó hoy en `diseno` (paso 42). Se aclaró primero una limitación técnica: las skills locales del vault (`web-design-engineer`, `ai-image-generator`, `color-palette`, `favicon-gen`, `icon-set-generator`, `image-processing`, `beautiful-article`, `web-video-presentation`) no aparecen invocables en esta sesión porque arrancó con `/Users/milo` como directorio de trabajo, no dentro del vault — se leyeron sus `SKILL.md` directamente desde `.claude/skills/` y se aplicó su metodología a mano. Se construyó un nuevo cliente demo, **Veterinaria Cocom** (`02-Clientes/demo-veterinaria-cocom/`, Perfil 2, paquete Completo, ficticio) recorriendo Ficha/Diagnóstico/Propuesta/Copy hasta Fase 4: `sitio-demo.html` con concepto "guardia encendida" (farol + huella animados, paleta eucalipto/terracota calculada con el algoritmo real de `color-palette`, favicon monograma de `favicon-gen`, set de 4 íconos consistentes de `icon-set-generator`, dossier de servicios sin cardificar siguiendo `web-design-engineer`/`failure-patterns.md`). Se probó `ai-image-generator` en vivo (`curl` directo a Gemini) — **sigue en `429`, cuota 0**, así que se usó ilustración SVG animada aplicando `emil-design-eng` (transform/opacity, easing propio, `prefers-reduced-motion`). Se verificó con Puppeteer en mobile y desktop, sin errores de JS, capturas enviadas al usuario. `producto-ia` diseñó el flujo de Typebot leyendo la Ficha/Diagnóstico/Copy del cliente (regla nueva del paso 42) en vez de una plantilla genérica. `ingenieria` generó `sitemap.xml` y `robots.txt` y documentó que el vínculo a Search Console no aplica sin dominio real. No se generaron los 3 documentos de cierre (ya validados dos veces con los demos anteriores) — todo documentado en `04-Desarrollo.md` del cliente demo.

48. El usuario invocó `/artifact-design` pidiendo fotos realistas, video corto y logo para el demo Veterinaria Cocom, con diseño dinámico usando ese material. Se señalaron dos conflictos antes de generar nada: fotos "realistas" chocan con la regla de `diseno` (nunca foto de stock/IA que finja ser real, precedente de AME Events) y están bloqueadas técnicamente (Gemini cuota 0, sin `OPENAI_API_KEY`); video solo es posible vía `higgsfield`, vetado permanentemente por el usuario. El usuario eligió mantener la regla del vault: ilustración/movimiento en vez de fotos. Se agregó un logo real (mark SVG reutilizable con `<use>`, lockup completo en footer, versión compacta en nav), se reemplazó el placeholder de foto de la doctora por una escena ilustrada nueva, y se agregó contador animado + paralaje sutil del hero (ambos respetan `prefers-reduced-motion`/`pointer:fine`). Se encontró y corrigió en el momento un bug propio (animación de glow mal heredada tapando la ilustración) — verificado con Puppeteer antes de cerrar.

49. El usuario aportó fotos y video reales en `/Users/milo/Documents/assets cocom vet/` (6 fotos + 1 video de stock real, patrón de nombre de Pexels, no IA) y pidió reconstruir el sitio del demo con ellos, con más movimiento, investigando primero sitios de alto impacto. Se optimizaron los activos (`image-processing`: fotos a 1000-1600px, video recortado a 8s/720px/sin audio, ~34MB → ~1.4MB) y se investigó tendencias 2026 (video hero full-bleed, tipografía sobredimensionada, reveals de scroll, moderación en negocio local). Se reconstruyó el hero como video full-bleed con overlay y tipografía más grande, se agregaron secciones nuevas con fotos reales (instalaciones, galería de pacientes) y se reemplazó la ilustración de "quién te atiende" por la foto real del equipo (ajustando el copy de "la doctora" a "la doctora y su equipo" por honestidad con la foto). Se encontraron y corrigieron dos bugs reales en el momento: un selector CSS que asumía ancestro/descendiente cuando las clases estaban en el mismo elemento (las fotos nunca aparecían) y un botón ilegible sobre el nuevo fondo oscuro de video. Verificado con Puppeteer en cada paso. Todo documentado en `04-Desarrollo.md` del cliente demo, incluida la nota de que son fotos de stock (no de un cliente real) usadas a petición explícita para esta prueba.

50. El usuario notó que faltaban los documentos de cierre del demo Veterinaria Cocom (sí se habían generado en los dos demos anteriores). Se completó el flujo de `contratacion`: `06-Ficha-de-Pago.pdf`, `07-Bienvenida.pdf`, `08-Agreement.pdf` con folio **DEMO-2026-003**, cada uno con un banner "DOCUMENTO DEMO" visible. Verificados con `screenshot.js` (1 página cada uno) y con `file` sobre el PDF real antes de borrar los `.html` intermedios. `00-Ficha.md` actualizada con la sección "Documentos de cierre".

52. **El usuario pidió agregar entregables por cliente al dashboard (PDF + sitio demo), un tutorial de acceso al panel, y una regla de credencial de portal al convertir prospecto→cliente — y correr una demo end-to-end para validarlo.** El dashboard admin/portal vive en un repo real (`github.com/milogonzhid-png/EE-solutions-web`, deploy en Netlify vía git, visible en `eesolutions.com.mx/app`) que el checkout local de `app/` **nunca tenía como git real** (era una copia de zip descargada dos veces — de ahí los archivos ` 2` duplicados en todo `app/`, no son trabajo divergente, son basura de zip). Al clonar el repo de verdad se encontró drift en ambas direcciones: GitHub tenía 2 migraciones de Supabase (`alta_admins_emilio_eduardo`, `permitir_a_authenticated_usar_funciones_rls`) que el checkout local no tenía, y el checkout local tenía trabajo de dashboard (componente `LineaDeFases`/`EmbudoDeFases`, detalle de las 6 fases en `fases.ts`) que nunca se había subido a GitHub. Se reconciliaron ambos lados en un solo commit en vez de perder alguno.
    - **Schema:** tabla nueva `entregables_cliente` (tipo ficha_pago/bienvenida/agreement/demo_web/otro, storage_path o url_externa) + bucket privado `entregables-clientes` en Supabase, RLS igual que `clientes`/`pendientes` (admin ve todo, cliente solo lo suyo vía `mi_cliente_id()`). Columna `correo` agregada a `clientes`. Migración aplicada a producción y versionada en `supabase/migrations/`.
    - **Dashboard/portal:** sección "Entregables" en `/dashboard/clientes/[slug]` y "Tus documentos" en `/portal`, con URL firmada (10 min) generada del lado del servidor — nunca un link público fijo, porque los PDF llevan precio y contrato.
    - **Scripts nuevos** en `~/Documents/ee-solutions-secrets/scripts/supabase-sync/`: `sync-entregables.js` (sube PDF/sitio demo a Storage + registra la fila, enganchado a `/contratacion`, `/convertir-prospecto` y `/sincronizar`) y `provisionar-credencial.js` (invita al portal — **dry-run por default**, exige `--confirmar` explícito después de que Emilio/Eduardo den el visto bueno, porque "exponer credenciales" es una de las 5 decisiones reservadas del consejo). Se corrigieron de paso dos bugs reales encontrados al correr la demo: `sync-clientes.js` nunca sincronizaba el `correo` nuevo, y el parser de frontmatter no quitaba comillas (`cliente: "Nombre"` llegaba con comillas literales al dashboard) — corregido en los 3 scripts que comparten ese parser.
    - **Tutorial de acceso:** página de clic-avance (`07-Recursos/tutorial-acceso-panel.html`, copiada a `app/public/`) explicando correo → enlace o código de 6 dígitos → portal. Vive en `eesolutions.com.mx/app/tutorial-acceso-panel.html`, enlazada desde `/login`. Es genérico, no por cliente — no se regenera salvo que cambie el flujo de login mismo. (Se intentó primero como HTML público en Supabase Storage; Supabase fuerza `Content-Type: text/plain` + CSP sandbox en objetos públicos por anti-XSS, así que no servía para una página interactiva — se movió a `app/public/` del propio dashboard.)
    - **CLAUDE.md:** nuevo §14 documentando los tres puntos de arriba; §4 agrega `correo:`/`demo_url:` al frontmatter y `sitio-demo.html` como archivo opcional del cliente.
    - **Demo end-to-end corrida y verificada:** cliente de prueba `demo-qa-taller-el-tornillo` (banner "cliente de prueba" en su propia ficha) recorriendo el pipeline real — Ficha con `correo:`, Propuesta con precio real del paquete Esencial, los 3 PDF generados con `export.js` (verificados leyendo el PDF real), `sitio-demo.html`, `sync-clientes.js` + `sync-entregables.js` corridos con éxito, fila en `entregables_cliente` confirmada por SQL, URL firmada probada con `curl` (200, `application/pdf`), y `provisionar-credencial.js` probado en dry-run (no mandó nada). **Deploy real a Netlify vía push a `main`** (commit `b357ed8`), confirmado `ready` y las dos rutas nuevas responden 200 en producción. **Queda para el usuario:** entrar al dashboard real y confirmar visualmente que `demo-qa-taller-el-tornillo` se ve bien antes de borrarlo (mismo criterio que todos los demos anteriores — no se deja mezclado con clientes reales en `02-Clientes/`; hoy no hay ninguno). Sigue pendiente además: decidir a futuro si `.raw/` — y en general `02-Clientes/` con datos reales de clientes — debe entrar a este mismo repo de git o quedar fuera (esta sesión solo commiteó `app/` y `supabase/migrations/`, nunca `obsidian/`).

53. El usuario reportó (con captura) que el sitio demo del cliente de prueba se veía como código fuente en el dashboard, no como página. Causa real: Supabase Storage fuerza `Content-Type: text/plain` + `Content-Security-Policy: sandbox` en **cualquier** objeto que sirve, público o firmado — anti-XSS deliberado de su lado, confirmado con `curl` sobre el signed URL real. Fix: nueva ruta `app/src/app/demo/[slug]/route.ts` que descarga el archivo del lado del servidor (con la sesión del usuario, RLS sigue aplicando) y lo re-sirve con `Content-Type: text/html` correcto; `entregablesConUrl()` ahora enruta los `demo_web` con archivo en Storage por `/demo/<slug>` en vez de generar un signed URL (los PDF no cambiaron, esos sí llegan con `application/pdf` real). Verificado con `npm run build` local antes de subir.
    - Al ir a pushear se encontró que **otra sesión/persona pusheó un commit nuevo mientras se trabajaba** (`0b45eba`, rediseño del login/panel con componentes `Logo`/`NavPanel`) — se reconcilió sin pisar nada: se clonó de nuevo, se verificó que no había conflicto real (mis 4 archivos no los tocó esa sesión), se aplicó el fix encima de esa base y se pusheó (`ad4e287`).
    - **Bloqueador nuevo, fuera del código:** ni ese commit de branding ni el fix de esta sesión llegaron a desplegarse — Netlify marca **cualquier** deploy de `ee-solutions-app` como `skipped`/`error` en menos de 1 segundo ahora mismo, tanto los disparados por push a `main` como un deploy manual por upload directo (probado). El sitio en vivo sigue sirviendo el deploy de antes de ambos cambios (`b357ed8`, `ready`). No es un problema de código — es de configuración/cuenta de Netlify (plan `nf_team_dev`, posible límite de minutos de build o un ignore-build-script mal configurado) y requiere entrar al dashboard de Netlify (`app.netlify.com/sites/ee-solutions-app` → Deploys) para diagnosticarlo o re-disparar el deploy a mano. **Queda pendiente para Emilio/Eduardo o quien tenga acceso al dashboard de Netlify.**

54. **El usuario pidió revertir la sincronización de entregables al dashboard** (construida y arreglada en los pasos 52-53) — los PDF de bienvenida y el sitio demo de cada cliente vuelven a ser **solo archivos locales**, en una carpeta nueva `02-Clientes/<slug>/Entregables/` (Finder/iCloud), sin dashboard, sin Supabase Storage, sin tabla, sin script de sincronización. Se revirtió todo lo construido para esa función:
    - **Vault:** `CLAUDE.md` §4/§4.1/§14 y el Anexo actualizados; `demo_url:` se quita del frontmatter (ya no tiene para qué existir); `/contratacion` y `/convertir-prospecto` ahora generan los 3 PDF (y el sitio demo, cuando existe) dentro de `Entregables/` en vez de sueltos en la raíz del cliente; `/sincronizar` vuelve a solo correr `sync-clientes.js`. Se borró `sync-entregables.js`.
    - **Supabase:** se dropeó la tabla `entregables_cliente` y el enum `tipo_entregable` (migración `revertir_entregables_cliente`), y se borró el bucket `entregables-clientes` completo (Storage API, con sus 4 objetos del cliente de prueba).
    - **Dashboard (`app/`):** se quitó la sección "Entregables" de `/dashboard/clientes/[slug]`, "Tus documentos" de `/portal`, la ruta `/demo/[slug]`, y `src/lib/entregables.ts`. `database.types.ts` regenerado limpio. Commit `dcfb4fd`, pusheado — **sigue bloqueado el mismo problema de Netlify del paso 53** (cualquier deploy de `ee-solutions-app` se marca `skipped` en <1s, tanto por push como manual; probado de nuevo con este commit, mismo resultado). No es nuevo, ya estaba documentado; el fix de este paso ya está en `main`, listo para cuando se resuelva Netlify.
    - **No se tocó** (no se pidió quitarlo): la credencial de acceso al portal (`clientes.correo` + `provisionar-credencial.js`, dry-run por default) y el tutorial de acceso publicado en `/login`.
    - Se borró el cliente de prueba `demo-qa-taller-el-tornillo` por completo (local + fila en Supabase `clientes`) — su único propósito era probar la sincronización que ya no existe, y su ficha tenía referencias desactualizadas al dashboard. `02-Clientes/` vuelve a estar vacío.

55. **El dashboard se migró de Netlify a Cloudflare Workers, y el dominio pasó a DNS de Cloudflare.** Cierra el bloqueo de los pasos 53-54: los deploys no fallaban por configuración, sino porque **la cuenta de Netlify agotó sus créditos del plan gratuito** ("production deploys are paused"). Todo commit desde las 4:57 AM aparecía como `skipped` — incluido el rediseño del panel y el fix de entregables.
    - **Causa de fondo:** cada build de Next.js consume créditos y el sitio estático casi no consume. Teniéndolos en la misma cuenta, el panel se comía el presupuesto de la web. La solución fue separarlos por proveedor, no pagar.
    - **Dashboard → Cloudflare Workers** con `@opennextjs/cloudflare` (worker `ee-solutions-web`, construido desde GitHub en cada push). Verificado antes de migrar: Next.js 16 soportado, y el bundle pesa 1.37 MiB comprimido de un límite de 3 MiB en el plan gratuito. Se eliminó `app/netlify.toml` y las menciones a Netlify en el código del panel.
    - **DNS del dominio → Cloudflare** (nameservers `cass`/`ken.ns.cloudflare.com`, cambiados en Hostinger). Se copiaron los 7 registros; **el escaneo automático de Cloudflare no detectó los CNAME `send` y `rsend` de Resend** y hubo que agregarlos a mano — si se hubieran perdido, los correos de acceso dejaban de enviarse. Los 4 registros A con IP fija de Netlify se reemplazaron por CNAME a `jocular-yeot-b76b98.netlify.app`.
    - **Ruta `eesolutions.com.mx/app/*` → worker** (Workers Route). Requiere que la raíz y `www` estén proxeadas (nube naranja). El sitio público **sigue en Netlify sin cambios**; solo esa ruta la atiende Cloudflare.
    - **Rediseño del panel** (que estaba congelado): logo SVG canónico de marca, tipografías del sitio público (Unbounded/Instrument Sans/JetBrains Mono), y las 6 fases con la progresión de color blanco → cyan → violeta → magenta de la sección "Proceso" de la web. Se corrigieron además los nombres de las fases 3-6, que no coincidían con `04-Proceso-6-Fases.md`.
    - **Costo actual de infraestructura: $0/mes.** Se descartó Vercel: su plan gratuito prohíbe uso comercial. Documentado en `01-Empresa/07-Stack-Tecnologico.md`.
    - **Pendiente:** borrar el sitio `ee-solutions-app` de Netlify (ya no lo usa nadie; libera créditos para la web).

## Anterior a eso
Vault recién inicializado. Sin trabajo previo.

## Pendientes
Ya no se llevan aquí — antes se repetían entre `_estado.md`, `Precios.md`, `Cobros.md` y se desactualizaban sin que nadie lo notara. Todo vive en un solo lugar: [[05-Operacion/00-Pendientes-Generales]].

**No es un pendiente, es una decisión ya tomada:** el desglose por componente **no existe** — Eduardo confirmó que solo hay precio por paquete completo (Esencial/Completo), no por pieza individual. Cerrado en `Precios.md` y `00-Precios-y-Costos.md` el 2026-08-21.

**Resuelto (histórico — ver pasos 20-24 arriba para el detalle):** los 2 paquetes con precio real (Esencial $6,300+$350/mes, Completo $8,700+$550/mes) y su modelo de costos completo, incluidas las horas por fase · costo de dominio (va incluido en el costo del paquete, no es renglón aparte) · datos bancarios (dejan de ser un "pendiente" fijo — es un campo que Emilio llena a mano en cada Ficha de Pago real, la plantilla ya lo deja listo) · el Agreement es simbólico y no necesita razón social/RFC ni revisión de abogado para usarse (esa revisión solo aplicaría si algún día se usa el contrato legal completo, que es la excepción) · `GEMINI_API_KEY` configurada y probada contra la API real · costo de Typebot = $0 (versión gratuita) · dirección física del cliente sí se publica en la web · calendario de contenido orgánico junto con [[contenido]] · WhatsApp oficial `951 212 8121` · skills de generación de imágenes instaladas ([[diseno]]).

## Decisiones recientes
_(las decisiones importantes se registran en `05-Operacion/Decisiones.md`)_
